source 'https://rubygems.org'

# Ruby version and default gemset.
ruby '2.0.0'
#ruby-gemset=dapi

# Nokogiri gem for XML and HTML traversing.
gem 'nokogiri'

# RSolr gem for interation with Solr servers.
gem 'rsolr'

# Pry gem for testing (and also because it's 927834234 times better than irb)
gem 'pry'
